# 🚀 Jiralite — Microservices Task Management System

**Jiralite** — это масштабируемая система управления задачами (аналог Jira), построенная на микросервисной архитектуре с использованием **Spring Boot** и **Kafka**. Проект реализует безопасную аутентификацию, асинхронные уведомления и контейнеризацию.

---

## 🛠 Технологический стек

* **Ядро:** Java 17, Spring Boot 3.2
* **Архитектура:** Microservices, Event-Driven
* **База данных:** PostgreSQL
* **Брокер сообщений:** Apache Kafka + Zookeeper
* **Безопасность:** Keycloak (OAuth2 / OpenID Connect)
* **Контейнеризация:** Docker, Docker Compose
* **Тестирование:** JUnit 5, Mockito, H2 Database (Integration Tests)
* **Документация:** OpenAPI (Swagger UI)

---

## 🏗 Архитектура системы

Проект состоит из двух микросервисов и инфраструктурных компонентов:

1.  **Jiralite (Main Service):** Отвечает за REST API, бизнес-логику, работу с БД и безопасность.
2.  **Notification Service:** Асинхронно обрабатывает события из Kafka для отправки уведомлений.
3.  **Infrastructure:** PostgreSQL, Kafka, Zookeeper и Keycloak, запущенные в Docker.

---

## 🚀 Как запустить

### 1. Запуск инфраструктуры (Docker)
Убедитесь, что Docker Desktop запущен. Выполните команду в корне проекта:

bash
docker-compose up -d


*Подождите 1-2 минуты для полной инициализации Keycloak и Kafka.*

### 2. Запуск микросервисов
Запустите приложения через IDE (IntelliJ IDEA) или терминал:
1.  **Notification Service** (`NotificationServiceApplication.java`)
2.  **Jiralite Main Service** (`JiraliteApplication.java`)

---

## 🔐 Безопасность и использование API

API защищено с помощью **Keycloak**. Для выполнения операций требуется Access Token.

### Учетные данные
* **Admin/Manager:** `manager` / `123`
* **Keycloak Console:** http://localhost:8180 (admin/admin)

### 1. Получение токена (Терминал)
Выполните команду для получения JWT токена:

bash
curl -d "client_id=jira-client" -d "username=manager" -d "password=123" -d "grant_type=password" "http://localhost:8180/realms/course-realm/protocol/openid-connect/token"


*Скопируйте `access_token` из ответа.*

### 2. Доступ к Swagger UI
Откройте документацию API:
👉 **http://localhost:8080/swagger-ui.html**

1.  Нажмите **Authorize**.
2.  Введите: `Bearer <ВАШ_ACCESS_TOKEN>`.
3.  Теперь вы можете безопасно выполнять `POST` и `GET` запросы.

---

## 🧪 Тестирование

Проект включает **Unit-тесты** (Mockito) и **Интеграционные тесты** (H2 Database), покрывающие:
* Бизнес-логику (Service Layer)
* Поток Kafka Producer/Consumer
* Ограничения безопасности
* Маппинг REST контроллеров

**Запуск всех тестов:**
bash
./mvnw test


---

## ✅ Чек-лист требований (Grading Requirements)

| Компонент | Статус | Описание |
| :--- | :---: | :--- |
| **REST API** | ✅ | Полный CRUD для Проектов/Задач с DTO. |
| **Database** | ✅ | PostgreSQL с реляционной моделью (1:N). |
| **Kafka** | ✅ | Асинхронная обработка событий между сервисами. |
| **Security** | ✅ | Интеграция Keycloak, доступ по ролям, валидация JWT. |
| **Docker** | ✅ | `docker-compose.yml` для оркестрации всех зависимостей. |
| **Testing** | ✅ | 7+ тестов, включая Unit и полные интеграционные сценарии. |
| **Code Quality** | ✅ | Чистая архитектура, использование DTO, логирование. |

---
